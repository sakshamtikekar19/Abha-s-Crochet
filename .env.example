# ===== Supabase (Auth + Database) =====
# Get these from https://supabase.com/dashboard → Project Settings → API
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key
# Service role / secret key (for orders, admin) - NEVER expose to client
# Option 1: Legacy key from Project Settings → API → service_role
# Option 2: New secret key from Project Settings → API Keys → Create secret key
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key
# Or use new format: SUPABASE_SECRET_KEY=sb_secret_xxxxx

# ===== Admin (login & session signing) =====
# Owner email and password for admin login at /admin/login
ADMIN_EMAIL=owner@example.com
ADMIN_PASSWORD=your-secure-password
# Secret for signing session cookies (any random string)
ADMIN_SECRET=your-admin-secret

# ===== Razorpay (Payments) =====
# Dashboard: https://dashboard.razorpay.com → Settings → API Keys
# Test keys (rzp_test_...): for development; no real money.
# Live keys (rzp_live_...): for production; real payments.
# Same variable names for both – just replace the values when going live.
NEXT_PUBLIC_RAZORPAY_KEY_ID=rzp_test_xxxxxxxx
RAZORPAY_KEY_SECRET=xxxxxxxx

# ===== Contact & Social =====
# WhatsApp: country code + number, no + (e.g. 919876543210 for India)
NEXT_PUBLIC_WHATSAPP_NUMBER=919876543210
NEXT_PUBLIC_WHATSAPP_DEFAULT_MESSAGE=Hi! I'm interested in your crochet products.

# Instagram: username without @
NEXT_PUBLIC_INSTAGRAM_HANDLE=yourhandle

# Optional: Brand
NEXT_PUBLIC_BRAND_NAME=Handmade Crochet
NEXT_PUBLIC_BRAND_TAGLINE=Handcrafted Crochet Made in India
NEXT_PUBLIC_CONTACT_EMAIL=hello@example.com

# ===== Order notification email =====
# Email address to receive order notifications (with full order & delivery details)
OWNER_EMAIL=owner@example.com
# Resend API key from https://resend.com → API Keys
RESEND_API_KEY=re_xxxxxxxx
# Sender email (must be verified in Resend; use onboarding@resend.dev for testing)
ORDER_EMAIL_FROM=onboarding@resend.dev
